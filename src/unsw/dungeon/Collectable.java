package unsw.dungeon;

public interface Collectable {
	/**
	 * allows player to collect the object
	 * @param p the player who wants to collect the object
	 */
    public void collect(Player p);
    

}
